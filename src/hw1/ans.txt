2. Определить сложность следующих алгоритмов:

-. Поиск элемента массива с известным индексом (O log2(n))
-. Дублирование одномерного массива через foreach (O(n))
-. Удаление элемента массива с известным индексом без сдвига (O log2(n+1))
-. Удаление элемента массива с неизвестным индексом без сдвига (O log2(n+1))
-. Удаление элемента массива с неизвестным индексом со сдвига (O log2(n+2))
.

3.
А) 13 итераций, так как во вложенном степени 2(вложенный цикл)
Первый пройдет все итерации до 10000.
б) 5000 итераций
с)5000 итераций
d)O(n)  - 10000 итераций, так как числа перемножаются последовательно
e)O(n) время выполнения, т.е. 10000 итераций